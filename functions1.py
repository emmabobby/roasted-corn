def len_of_string(lexus):
	return len(lexus)

def characters(divine):
	count = 1
	word = ""
	while count < len(                                       `):
		word += divine[count]
		count += 2
	return word

def minimum(numbers):
	min = numbers[0]
	for num in numbers:
		if num < min:
			min = num
	return min
def maximum(numbers):
	max = numbers[0]
	for num in numbers:
		if num > max:
			max = num
	return max
def sum_square_elements(smart):
	total =
	for num in smart:
		sqr = num**2
		total +=sqr
	return total
def square_elements(harry):
	result = []
	for num in harry:
		sqr = num**2
		result.append(sqr)
	return result

def words(harry,number):
	tobi = ""
	if type(number)== float:
		return harry
	else:
		name = harry * number
		tobi += name
	return tobi
			
print(words("hello",4.5))
		
	
